<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.jcc.seven.Translate">
    <TableLayout android:stretchColumns="1"
        android:padding="10dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <TableRow>
            <TextView android:text="@string/from_text" />
            <Spinner android:id="@+id/from_language"></Spinner>
        </TableRow>
        <EditText android:id="@+id/original_text"
            android:hint="@string/original_hint"
            android:padding="10dp"
            android:textSize="18sp"/>
        <TableRow>
            <TextView android:text="@string/to_text" />
            <Spinner android:id="@+id/to_language"></Spinner>
        </TableRow>
        <TextView android:id="@+id/translated_text"
            android:padding="10dp"
            android:textSize="18sp"/>
        <TextView android:text="@string/back_text" />
        <TextView android:id="@+id/retranslated_text"
            android:padding="10dp"
            android:textSize="18sp"/>
    </TableLayout>

</ScrollView>
